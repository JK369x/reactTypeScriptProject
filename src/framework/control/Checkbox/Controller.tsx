import { FC } from 'react'

const ControllerCheckbox: FC = () => {
	return (
		<></>
		// <FormControlLabel
		// 	control={<MuiCheckbox checked={checked} />}
		// 	label={t('useCredit')}
		// 	onChange={(e: any) => {
		// 		setValue('useCredits', e.target.checked)
		// 	}}
		// />
	)
}
